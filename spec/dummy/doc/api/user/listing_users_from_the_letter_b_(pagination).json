{
  "resource": "User",
  "http_method": "GET",
  "route": "/users",
  "description": "Listing users from the letter B (pagination)",
  "explanation": "This uses the paginative gem for pagination by showing only the records after the record you pass in.",
  "parameters": [
    {
      "required": false,
      "scope": "from",
      "name": "name",
      "description": "Name of the last record on the previous page"
    }
  ],
  "response_fields": [

  ],
  "requests": [

  ]
}